# Private notes (copy to Drive manually; not pushed)
private/

# Leftover if create-astro targeted a non-empty dir (safe to delete locally)
dimensional-debris/

# Dependencies & build
node_modules/
dist/
.astro/

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Env (keep .env.example tracked as a template)
.env
.env.local
.env.production
